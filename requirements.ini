# Core scientific computing
numpy>=1.21.0

# For potential future STARK integration (using a placeholder library)
# winterfell>=0.5.0

# For JSON configuration handling
# (No extra library needed if using standard `json` module)